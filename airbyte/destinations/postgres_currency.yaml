definition:
  name: "postgres_currency"
  destinationType: "postgres"
  config:
    host: "localhost"
    port: 5432
    database: "${DB_NAME}"
    username: "${DB_USER}"
    password: "${DB_PASSWORD}"
    ssl: true
